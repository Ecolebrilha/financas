# SQLite (padrão, zero config)
DATABASE_URL="file:./dev.db"

# Postgres (opcional) - troque o provider em prisma/schema.prisma para
# "postgresql" e descomente a linha abaixo com seus dados de conexão.
# DATABASE_URL="postgresql://usuario:senha@localhost:5432/financas?schema=public"

PORT=3001
